{
  "version": 2,
  "builds": [
    {
      "src": "FrontEnd/vite-project/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/FrontEnd/vite-project/$1" }
  ]
}
